apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tennis-reg-tls-cert
  namespace: default
spec:
  secretName: tennis-reg-tls-cert
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  commonName: rareshoro.site
  dnsNames:
  - rareshoro.site
